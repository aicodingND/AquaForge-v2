{
  "servers": {
    "cloudrun": {
      "enabled": true,
      "defer_loading": true,
      "description": "Google Cloud Run deployment - lazy loaded to save tokens"
    },
    "firebase-mcp-server": {
      "enabled": true,
      "defer_loading": true,
      "description": "Firebase operations - lazy loaded to save tokens"
    },
    "sequential-thinking": {
      "enabled": true,
      "defer_loading": false,
      "description": "Problem decomposition - always available for PRISM workflow"
    }
  },
  "optimization": {
    "defer_loading_default": true,
    "lazy_tool_discovery": true,
    "token_savings_mode": true,
    "notes": "MCP servers configured for ~85% token reduction. Tools discovered on-demand."
  }
}
