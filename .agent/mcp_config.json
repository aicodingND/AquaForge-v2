{
  "servers": {
    "cloudrun": {
      "enabled": true,
      "defer_loading": true,
      "description": "Google Cloud Run deployment - lazy loaded to save tokens"
    },
    "firebase-mcp-server": {
      "enabled": true,
      "defer_loading": true,
      "description": "Firebase operations - lazy loaded to save tokens"
    },
    "sequential-thinking": {
      "enabled": true,
      "defer_loading": false,
      "description": "Problem decomposition - always available for PRISM workflow"
    },
    "playwright": {
      "enabled": true,
      "defer_loading": true,
      "description": "Browser automation and E2E testing - use as needed"
    },
    "github": {
      "enabled": true,
      "defer_loading": true,
      "description": "GitHub API operations - use as needed (requires GITHUB_PERSONAL_ACCESS_TOKEN)"
    }
  }
}
